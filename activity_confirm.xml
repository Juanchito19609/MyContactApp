<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:padding="16dp"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <TextView android:id="@+id/tvName" android:textSize="18sp"/>
    <TextView android:id="@+id/tvDate" android:textSize="18sp"/>
    <TextView android:id="@+id/tvPhone" android:textSize="18sp"/>
    <TextView android:id="@+id/tvEmail" android:textSize="18sp"/>
    <TextView android:id="@+id/tvDescription" android:textSize="18sp"/>

    <Button android:id="@+id/btnEdit"
        android:layout_marginTop="24dp"
        android:text="Editar datos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
</LinearLayout>